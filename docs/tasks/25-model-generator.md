# Task: 25-model-generator

Goal: the "core engine" story — how a NEW repo gets its `data/project.json`. Two deliverables, no server code:

1. `docs/ONBOARDING.md` (≤ 150 lines): the workflow. Inputs: repo tree, package manifests, README/docs, tests, git log / PR descriptions / ADRs, and the human's conversation. Steps: (a) propose categories as stages from the dependency flow (folders + imports); (b) propose systems = module boundaries with `paths` + evidenced edges (imports/calls); (c) draft requirements from README/tests/issue titles (kind incl. feature), status from tests/CI; (d) draft intents from commit messages, PR bodies, ADRs, code comments that say "because"; (e) EVERYTHING lands `ai-inferred`; (f) human verification loop: the tool shows advisories + AI-inferred pills, the human confirms/edits → `human-verified` with sessionRef; (g) `validate:data` gates the write; (h) ongoing capture: each coding session appends (this repo's rule 9 is the prototype of that). Include what to do when a repo is huge (per-package passes, category cap), and honesty rules (never invent intent; unknown → ai-inferred with low-confidence wording, or a gap).
2. A runnable Claude Code skill: `.claude/skills/capture-model/SKILL.md` (frontmatter: name `capture-model`, description "Generate or update data/project.json for this repo following docs/MODELING.md") that walks steps a–g interactively, cites evidence per entry, appends only, runs `npm run validate:data` (or the equivalent if the target repo isn't this one — describe how to vendor `shared/` + the script), and asks the human to verify each `ai-inferred` batch before writing `human-verified`. Reference MODELING.md rules verbatim where it matters (limits, summary ≤ 20 words). Keep it under 200 lines; it must be usable from a fresh repo with only this skill + the schema.
3. Seed: system `sys-capture-workflow` (kind workflow, category Workflow, paths `docs/ONBOARDING.md`, `.claude/skills/capture-model/SKILL.md`); edge capture-workflow → seed-data (writes, "generates and appends"), edge agent-rules → capture-workflow (depends: rule 9 is the ongoing half); intent `int-capture-is-the-product` — "The viewer is the read side; the capture workflow — an LLM proposing, a human verifying, provenance recorded — is the product's core engine" (human-verified, sessionRef "planning chat 2026-08-27, wrap-up"), appliesTo sys-capture-workflow, sys-seed-data, sys-agent-rules. CLAUDE.md: one line pointing to ONBOARDING.md. README not yours (task 09).
   Own: `docs/ONBOARDING.md`, `.claude/skills/capture-model/**`, `CLAUDE.md` (one line), seed appends. Merge on green report (no inspection gate).
