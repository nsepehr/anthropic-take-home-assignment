# Task: 24-feature-kind

Goal: "features" exist in the model without a new entity: `Requirement.kind` gains `'feature'` (a user-facing capability), and the UI shows it.

- `shared/src/schema/requirement.ts`: `RequirementKind = z.enum(['feature','functional','non-functional','constraint'])` (additive). `docs/MODELING.md`: one paragraph — a feature is what a user can do ("A user can open a system and walk its neighbours"); functional = what the system must do internally; constraint/non-functional unchanged. Keep `.tag-kind` pill; add `--pill-kind-feature-bg/fg` (accent-100 / accent-900) and `.tag-kind-feature` so features read as a distinct pill.
- Seed: retag as `feature` the requirements that are user capabilities (e.g. req-system-focus, req-single-focused-interaction, req-one-view-understanding, req-overview-deep-dive, req-category-lanes — judge each honestly; leave constraints alone). Append intent `int-feature-is-a-requirement-kind` — "A feature is a requirement of kind feature, not a fourth entity: it keeps one list and one linking rule, and the pill makes it visible" (human-verified, sessionRef "planning chat 2026-08-27, wrap-up"), appliesTo sys-shared-model, sys-client-panel. Append at END. Backlog line in TASKS.md: "Feature entity" → replaced by kind; note grouping requirements by feature as a later view.
- Panel: RequirementCard uses the feature pill; ProjectOverview/EntityDetail list order: features first, then the rest (pure sort helper, tested).
  Own: `shared/src/schema/requirement.ts`, `docs/MODELING.md` (one paragraph), `data/project.json` (retags + intent), `client/src/styles/tokens.css`+`base.css` (feature pill), `RequirementCard.tsx` (pill class), `client/src/model/entities.ts` (sort helper + test). Merge on green report (no inspection gate); screenshot `features.jpg`.
