/** The card's design width. Every view lays cards out at it; `.diagram-card` renders at it. */
export const CARD_WIDTH = 210;

/** Height the focus view lays its neighbour cards out with: what a card renders at. */
export const CARD_HEIGHT = 152;
