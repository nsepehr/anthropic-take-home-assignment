# Copy to .env.local (or run `npm run ports`, which writes it for you).
CLIENT_PORT=5173
SERVER_PORT=3001
# Path to the project model the server serves (default: data/project.json at the repo root).
# PROJECT_FILE=data/project.json
