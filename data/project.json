{
  "name": "Codebase Map",
  "mission": "A post-IDE interface for understanding a codebase: systems, their connections, the requirements they serve, and the human intent behind them — in one view.",
  "systems": [
    {
      "id": "sys-shared-model",
      "name": "Shared data model",
      "kind": "module",
      "category": "Model",
      "paths": [
        "shared/src/schema/",
        "shared/src/validate.ts",
        "shared/src/gaps.ts",
        "shared/src/related.ts",
        "shared/src/index.ts"
      ],
      "summary": "The contract between server and client: Zod schemas for System, Requirement, Intent and Edge, plus validation and graph helpers.",
      "detail": "Package `@app/shared`. Defines the four entity schemas with Zod and infers the TypeScript types from them, so there is exactly one definition. `validateProject()` checks shape and then referential integrity (unique ids, references resolving to the right entity type, edge endpoints existing, no `parentId` cycles) and returns readable path+message errors. `computeGaps()` reports what the model cannot explain. `relatedTo()` returns everything linked to an entity in either direction — the closure the UI will highlight on selection. Has no runtime dependency except `zod`. Built by task 01-schema-and-seed.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T15:00:00+08:00",
        "sessionRef": "docs/tasks/01-schema-and-seed.md"
      }
    },
    {
      "id": "sys-server-api",
      "name": "Server API",
      "kind": "service",
      "category": "Server",
      "paths": [
        "server/src/app.ts",
        "server/src/index.ts",
        "server/src/config.ts",
        "server/src/stores/projectStore.ts",
        "server/src/routes/"
      ],
      "summary": "A thin Fastify server that loads `data/project.json` once, validates it with `@app/shared`, and serves the whole project as one JSON payload.",
      "detail": "`stores/projectStore.ts` reads the file named by `PROJECT_FILE` (default `data/project.json`) at startup, runs `validateProject()` and caches the result; if validation fails the server still boots and reports the errors. Routes in `server/src/routes/`: `GET /api/project` (the validated Project — the same shape as `data/project.json` — or 500 with readable errors), `GET /api/health` (adds `project: { loaded, name }`), and dev-only `POST /api/project/reload` (absent when `NODE_ENV=production`). `app.ts` is the composition root; `config.ts` is the only reader of `process.env`. Built by task 02-api; per-entity routes (`/gaps`, `/related/:id`, `/entities/:id`) were removed by 02b-api-trim after human review.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T18:00:00+08:00",
        "sessionRef": "docs/tasks/02-api.md (incl. Revision)"
      }
    },
    {
      "id": "sys-client-app",
      "name": "Client app",
      "kind": "ui",
      "category": "Client",
      "paths": [
        "client/src/App.tsx",
        "client/src/main.tsx",
        "client/src/api/project.ts",
        "client/src/model/toFlow.ts",
        "client/src/features/debug/",
        "client/vite.config.ts"
      ],
      "summary": "React app that turns the project model into an interactive architecture diagram.",
      "detail": "Vite + React 18. `App.tsx` is a composition root wiring providers and the current top-level feature; `api/` is the only place `fetch` is called (responses parsed with `validateProject`); `model/toFlow.ts` maps Systems/Edges to React Flow elements; `features/debug/` is a throwaway page proving the pipeline until the designed UI replaces it.",
      "provenance": {
        "source": "ai-inferred",
        "capturedAt": "2026-08-27T15:00:00Z",
        "sessionRef": "agent: client-foundation (task 03)"
      }
    },
    {
      "id": "sys-seed-data",
      "name": "Seed data",
      "kind": "store",
      "category": "Model",
      "paths": ["data/project.json", "data/README.md"],
      "summary": "The hand-written record of this project, validated against the shared schema; it is the tool's own demo content.",
      "detail": "`data/project.json` conforms to `ProjectSchema`. It is not a mock: every entry describes the repo as it exists, captured from the planning conversation and the commit history, with `provenance` saying whether a human verified it or an AI inferred it. Every task appends its systems, requirements and intents here (CLAUDE.md rule 9) and runs `npm run validate:data`, which also prints the gaps report.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T15:00:00+08:00",
        "sessionRef": "docs/tasks/01-schema-and-seed.md"
      }
    },
    {
      "id": "sys-agent-workflow",
      "name": "Agent workflow",
      "kind": "workflow",
      "category": "Workflow",
      "paths": ["CLAUDE.md", "docs/AGENT_WORKFLOW.md", "docs/tasks/", "scripts/"],
      "summary": "The rules and scripts that let several AI coding agents work on this repo in parallel without colliding.",
      "detail": "Each task is a brief in `docs/tasks/<slug>.md` that names the files the agent owns. The agent runs in its own git worktree and branch (`agent/<slug>`) with its own allocated ports, must keep `npm run check` green, and finishes with `scripts/finish-worktree.sh`, which rebases on `main` and re-runs the checks. Merges are fast-forward only and done by the human. `CLAUDE.md` holds the short rules; `docs/AGENT_WORKFLOW.md` the long form.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T14:35:46+08:00",
        "sessionRef": "commit 47b4a68 (bootstrap); CLAUDE.md"
      }
    },
    {
      "id": "sys-worktree-scripts",
      "name": "Worktree scripts",
      "kind": "module",
      "parentId": "sys-agent-workflow",
      "paths": ["scripts/new-worktree.sh", "scripts/finish-worktree.sh"],
      "summary": "Shell scripts that open a task worktree and close it out with a rebase and full check run.",
      "detail": "`new-worktree.sh <slug>` creates branch `agent/<slug>` from `main`, adds a worktree at `../worktrees/<slug>`, installs dependencies and allocates ports. `finish-worktree.sh` refuses uncommitted changes, rebases onto `main`, runs `npm run check` and prints the fast-forward merge commands. Neither script merges or pushes.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T14:35:46+08:00",
        "sessionRef": "commit 47b4a68 (bootstrap)"
      }
    },
    {
      "id": "sys-ports-allocator",
      "name": "Ports allocator",
      "kind": "module",
      "parentId": "sys-agent-workflow",
      "paths": ["scripts/ports.mjs", "scripts/env.mjs"],
      "summary": "Gives each worktree its own free CLIENT_PORT and SERVER_PORT, written to a gitignored `.env.local`.",
      "detail": "`scripts/ports.mjs` reads the `.env.local` of every sibling worktree (via `git worktree list`) so it never hands out a port another agent has claimed, then probes the OS on IPv4, IPv6 and unspecified hosts before accepting a port. It is idempotent: re-running keeps existing ports unless `--force` is passed. `env.mjs` is the tiny dependency-free `.env.local` parser shared with the dev runner.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T14:35:46+08:00",
        "sessionRef": "commit 47b4a68 (bootstrap)"
      }
    },
    {
      "id": "sys-dev-runner",
      "name": "Dev runner",
      "kind": "module",
      "parentId": "sys-agent-workflow",
      "paths": ["scripts/dev.mjs"],
      "summary": "`npm run dev`: allocates ports, then starts the server and client dev servers with those ports in the environment.",
      "detail": "`scripts/dev.mjs` first runs the ports allocator synchronously, loads `.env.local`, prints the two URLs, and spawns `npm run dev` in the `server` and `client` workspaces. It forwards SIGINT/SIGTERM to both children and exits if either child fails. The Vite dev server proxies `/api` to the Fastify port.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T14:35:46+08:00",
        "sessionRef": "commit 47b4a68 (bootstrap)"
      }
    },
    {
      "id": "sys-check-suite",
      "name": "Check suite",
      "kind": "workflow",
      "parentId": "sys-agent-workflow",
      "paths": ["package.json", "eslint.config.js", "vitest.workspace.ts", "tsconfig.base.json"],
      "summary": "`npm run check` — typecheck, lint, format check and Vitest across all three workspaces; the definition of green.",
      "detail": "Root scripts run `tsc --noEmit` per workspace, ESLint (flat config, with a lint rule banning `dangerouslySetInnerHTML`), Prettier in check mode and `vitest run` over the `client`, `server` and `shared` workspaces. Every module ships with a small test that pins its intent so a parallel agent cannot silently break it. `finish-worktree.sh` runs this suite after rebasing.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T14:35:46+08:00",
        "sessionRef": "commit 47b4a68 (bootstrap); CLAUDE.md rule 6"
      }
    },
    {
      "id": "sys-client-state",
      "name": "Client state",
      "kind": "module",
      "parentId": "sys-client-app",
      "paths": [
        "client/src/state/projectStore.tsx",
        "client/src/state/selection.tsx",
        "client/src/model/selection.ts",
        "client/src/state/viewMode.tsx"
      ],
      "summary": "Project, selection, and view-mode state exposed as React hooks.",
      "detail": "`ProjectProvider`/`useProject` load the project once via the API. `SelectionProvider`/`useSelection` hold one selected id and derive `related`, `isHighlighted`, `isDimmed` with `relatedTo` from the shared model — no network round-trip. `useViewMode` is the global overview/deep-dive switch persisted in localStorage.",
      "provenance": {
        "source": "ai-inferred",
        "capturedAt": "2026-08-27T15:00:00Z",
        "sessionRef": "agent: client-foundation (task 03)"
      }
    },
    {
      "id": "sys-client-layout",
      "name": "Client layout",
      "kind": "module",
      "parentId": "sys-client-app",
      "paths": ["client/src/layout/elk.ts", "client/src/layout/useLayout.ts"],
      "summary": "Pure ELK layout pipeline: flow elements in, positioned nodes out.",
      "detail": "`layoutWithElk` builds a compound ELK graph (children nested by `parentId`, `INCLUDE_CHILDREN` so cross-group edges route) and returns nodes with parent-relative positions and ELK-computed parent sizes. `useLayout` wraps the async call and reports `idle | layouting | ready | error`.",
      "provenance": {
        "source": "ai-inferred",
        "capturedAt": "2026-08-27T15:00:00Z",
        "sessionRef": "agent: client-foundation (task 03)"
      }
    },
    {
      "id": "sys-client-styles",
      "name": "Client styles",
      "kind": "module",
      "parentId": "sys-client-app",
      "paths": ["client/src/styles/", "client/src/components/"],
      "summary": "The Organic design tokens and base classes, plus the shared presentational components that consume them.",
      "detail": "`styles/tokens.css` holds the `:root` variables ported verbatim from `docs/design/organic.css` plus app semantic tokens (`--kind-*`, `--status-*`, `--prov-*`, `--dim-opacity`, `--canvas-*`) derived from the ramps. `styles/base.css` carries the base rules and `.btn/.tag/.card/.seg` classes; `styles/index.css` is the single stylesheet entry. `components/` has `Tag`, `Button`, `KindDot`, `StatusDot`, `ProvenanceDot` — presentational only, typed from `@app/shared`.",
      "provenance": {
        "source": "ai-inferred",
        "capturedAt": "2026-08-27T20:00:00+08:00",
        "sessionRef": "agent: design-tokens (task 04)"
      }
    },
    {
      "id": "sys-client-lanes",
      "name": "Category lanes",
      "kind": "ui",
      "parentId": "sys-client-app",
      "paths": [
        "client/src/features/diagram/components/LaneLayer.tsx",
        "client/src/model/lanes.ts",
        "client/src/layout/useLanes.ts"
      ],
      "summary": "Draws a translucent lane with an uppercase label behind each category of systems.",
      "detail": "`model/lanes.ts` is pure: `categoryOf` walks `parentId` to the top-level ancestor, `laneOrder` orders categories by first appearance, `laneBounds` pads the positioned top-level nodes of each category. `useLanes` turns that into the `partitionOf` callback `useLayout` passes to ELK and the lane rectangles; `LaneLayer` renders them inside React Flow's `ViewportPortal` (z-index below nodes) so they pan and zoom with the graph. Built by task 08-category-lanes.",
      "provenance": {
        "source": "ai-inferred",
        "capturedAt": "2026-08-27T16:00:00+08:00",
        "sessionRef": "agent: category-lanes (task 08)"
      }
    },
    {
      "id": "sys-client-shell",
      "name": "App shell",
      "kind": "ui",
      "parentId": "sys-client-app",
      "paths": ["client/src/features/shell/", "client/src/App.tsx"],
      "summary": "The single-screen frame: header, canvas region and the always-visible 400px right panel with overview/detail slots.",
      "detail": "`AppShell` is a 100vh flex column (no page scroll): `Header` shows the project name, mission, the provenance legend and the global Overview / Deep dive `.seg` control bound to `useViewMode()`; `CanvasFrame` is the rounded dotted-grid region that renders the `canvas` slot and a hint while nothing is selected; `SidePanel` scrolls on its own and switches on `useSelection().selectedId` between the `overview` and `detail` slots. Slots are props so the shell never imports other features; the canvas slot is a render prop that receives the loaded project. Layout-only CSS lives in `shell.css`; every color, font and radius is a token from `client/src/styles`.",
      "provenance": {
        "source": "ai-inferred",
        "capturedAt": "2026-08-27T16:00:00+08:00",
        "sessionRef": "agent: app-shell (task 05)"
      }
    },
    {
      "id": "sys-client-panel",
      "name": "Detail panel",
      "kind": "ui",
      "parentId": "sys-client-app",
      "paths": ["client/src/features/panel/", "client/src/model/entities.ts"],
      "summary": "The right panel: the selected entity's what / must-do / why, or the whole project when nothing is selected.",
      "detail": "`EntityDetail` dispatches on the selected id: a headline `DetailCard` (kicker, title, summary, and in deep dive the system's how-it-works, the requirement's evidence, or the intent's rationale), then the entity's requirements and intents as clickable cards, then `ConnectedChips` for every linked entity. `ProjectOverview` shows mission, counts and every requirement and intent. The pure lookups (`findEntity`, `requirementsFor`, `intentsFor`, `connectionsFor`) live in `client/src/model/entities.ts` and derive everything from `relatedTo`.",
      "provenance": {
        "source": "ai-inferred",
        "capturedAt": "2026-08-27T16:00:00+08:00",
        "sessionRef": "agent: detail-panel (task 07)"
      }
    },
    {
      "id": "sys-client-diagram",
      "name": "Diagram",
      "kind": "ui",
      "parentId": "sys-client-app",
      "paths": ["client/src/features/diagram/"],
      "summary": "The architecture canvas: designed system cards and arrowed edges, laid out by ELK, lit by the selection.",
      "detail": "`DiagramCanvas` wraps React Flow with `nodeTypes={{ system: SystemNode }}` and `edgeTypes={{ system: SystemEdge }}`, sizes leaf nodes per view mode (`cardSize.ts`) before `useLayout`, and maps clicks to `useSelection().select/clear`. `SystemNode` renders a `SystemCard` (kind dot, name, summary or detail, `kind · N req · N why · Verified/AI` tags) or a `GroupCard` frame for parents. `SystemEdge` is a bezier with an arrowhead marker, accent when related and neutral otherwise; labels only in deep dive. All colours come from tokens via `diagram.css`; `elementState` (selected | related | dimmed | idle) is the one pure function both cards and edges use.",
      "provenance": {
        "source": "ai-inferred",
        "capturedAt": "2026-08-27T16:00:00+08:00",
        "sessionRef": "agent: system-node (task 06)"
      }
    }
  ],
  "requirements": [
    {
      "id": "req-self-contained-demo",
      "title": "Self-contained evaluation / demo mode",
      "kind": "constraint",
      "status": "partial",
      "systemIds": ["sys-server-api", "sys-seed-data"],
      "evidence": [
        "data/project.json",
        "server/src/projectStore.ts",
        "server/src/routes/project.ts (GET /api/project)"
      ],
      "summary": "A reviewer can run and evaluate the prototype without external services or credentials.",
      "detail": "From the assignment. The app must work from a clone with no API keys: the server serves the checked-in `data/project.json` rather than analysing a repo live or calling an LLM. The server now serves the validated seed as one payload; the client does not render them yet and nothing is deployed, so this is partial.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T15:10:00+08:00",
        "sessionRef": "claude-code session 2026-08-27, planning chat"
      }
    },
    {
      "id": "req-deployed-prototype",
      "title": "Deployed prototype",
      "kind": "constraint",
      "status": "planned",
      "systemIds": ["sys-server-api", "sys-client-app"],
      "evidence": ["CLAUDE.md (Stack: 'Deploy target later: Fly.io (not set up yet)')"],
      "summary": "The prototype is reachable at a public URL, not only on a laptop.",
      "detail": "From the assignment. The chosen target is Fly.io, but nothing is configured: there is no Dockerfile or `fly.toml`, and the server binds `127.0.0.1` in dev. Deployment is a later task.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T15:00:00+08:00",
        "sessionRef": "claude-code session 2026-08-27, planning chat"
      }
    },
    {
      "id": "req-single-focused-interaction",
      "title": "One single, focused hero interaction",
      "kind": "functional",
      "status": "partial",
      "systemIds": ["sys-client-app", "sys-shared-model", "sys-client-state"],
      "evidence": [
        "shared/src/related.ts",
        "shared/src/related.test.ts",
        "client/src/model/selection.ts",
        "client/src/features/diagram/"
      ],
      "summary": "Select one thing and everything connected to it — systems, requirements, intents, edges — lights up.",
      "detail": "From the assignment's guidance to do one interaction well. The chosen interaction is select-to-highlight. The data side is done: `relatedTo(project, id)` returns the closure in both directions and is tested. The UI side does not exist yet, hence `partial`.",
      "provenance": {
        "source": "ai-inferred",
        "capturedAt": "2026-08-27T15:00:00+08:00",
        "sessionRef": "task 01-schema-and-seed: status inferred from relatedTo existing without any UI"
      }
    },
    {
      "id": "req-written-video-rationale",
      "title": "Written and video rationale",
      "kind": "constraint",
      "status": "planned",
      "systemIds": [],
      "evidence": [],
      "summary": "The submission includes a written explanation and a short video walking through the design decisions.",
      "detail": "From the assignment. Nothing exists yet. Because the seed data records intents with rationale and rejected alternatives, the written rationale can largely be generated from — or at least cross-checked against — `data/project.json`.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T15:00:00+08:00",
        "sessionRef": "claude-code session 2026-08-27, planning chat"
      }
    },
    {
      "id": "req-ai-transcripts",
      "title": "AI transcripts submitted",
      "kind": "constraint",
      "status": "planned",
      "systemIds": [],
      "evidence": [],
      "summary": "The conversations with AI coding agents used to build the prototype are part of the submission.",
      "detail": "From the assignment. No system in the repo serves this; it is a process obligation on the human. The `sessionRef` field on every entity's provenance is the model's hook for pointing at those transcripts once they are collected.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T15:00:00+08:00",
        "sessionRef": "claude-code session 2026-08-27, planning chat"
      }
    },
    {
      "id": "req-one-view-understanding",
      "title": "Understand architecture, features and intent from one view",
      "kind": "functional",
      "status": "planned",
      "systemIds": ["sys-client-app"],
      "evidence": [],
      "summary": "A user opens the app and sees systems, the requirements they serve and the intent behind them together, without reading code.",
      "detail": "The product's core requirement. Systems render as boxes and edges as arrows; requirements and intents are front-and-center content on and around the nodes, not tooltips. The client currently renders a hello page, so this is entirely ahead of us; the model that makes it possible is in place.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T15:00:00+08:00",
        "sessionRef": "claude-code session 2026-08-27, planning chat"
      }
    },
    {
      "id": "req-overview-deep-dive",
      "title": "Overview and deep-dive language for every entity",
      "kind": "functional",
      "status": "partial",
      "systemIds": ["sys-shared-model", "sys-client-state"],
      "evidence": [
        "shared/src/schema/entity.ts (EntityBase: summary + detail required)",
        "client/src/state/viewMode.tsx"
      ],
      "summary": "Every system, requirement and intent can be read as a one-sentence overview or a deep dive, toggled globally.",
      "detail": "The schema requires `summary` and `detail` on every first-class entity, and the seed fills both. The global overview/deep-dive toggle in the UI is not built. Partial.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T15:00:00+08:00",
        "sessionRef": "claude-code session 2026-08-27, planning chat"
      }
    },
    {
      "id": "req-provenance-on-claims",
      "title": "Every claim shows its provenance",
      "kind": "non-functional",
      "status": "partial",
      "systemIds": ["sys-shared-model", "sys-seed-data"],
      "evidence": [
        "shared/src/schema/provenance.ts (ProvenanceSchema)",
        "shared/src/gaps.ts",
        "data/project.json"
      ],
      "summary": "For each entity the user can see whether a human verified it or an AI inferred it, when, and from where.",
      "detail": "Schema and seed side is done: `provenance` is required on every entity and the seed mixes `human-verified` and `ai-inferred` entries honestly. `computeGaps` surfaces what the model cannot explain. The UI badge and gaps panel are not built. Partial.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T15:00:00+08:00",
        "sessionRef": "claude-code session 2026-08-27, planning chat"
      }
    },
    {
      "id": "req-parallel-agents-safe",
      "title": "Parallel agents can't silently break each other",
      "kind": "non-functional",
      "status": "implemented",
      "systemIds": [
        "sys-agent-workflow",
        "sys-worktree-scripts",
        "sys-ports-allocator",
        "sys-check-suite",
        "sys-shared-model"
      ],
      "evidence": [
        "scripts/new-worktree.sh",
        "scripts/ports.mjs",
        "scripts/finish-worktree.sh",
        "CLAUDE.md rules 1, 2, 6",
        "shared/src/*.test.ts"
      ],
      "summary": "Several AI agents build features at the same time; isolation plus tests catch collisions before merge.",
      "detail": "Served by three mechanisms that all exist today: isolated worktrees with their own branch and ports; intent-pinning tests per module run by `npm run check`; and `finish-worktree.sh`, which rebases on `main` and re-runs the suite before a merge is allowed. Contract changes in `shared/` must be called out in each report.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T15:00:00+08:00",
        "sessionRef": "claude-code session 2026-08-27, planning chat; CLAUDE.md"
      }
    },
    {
      "id": "req-dogfood-seed",
      "title": "The seed data describes this project itself and stays current",
      "kind": "functional",
      "status": "implemented",
      "systemIds": ["sys-seed-data", "sys-shared-model"],
      "evidence": [
        "data/project.json",
        "data/README.md",
        "package.json (validate:data)",
        "CLAUDE.md rule 9"
      ],
      "summary": "`data/project.json` is a validated, honest record of this repo that every task appends to.",
      "detail": "Implemented by this task: the seed exists, validates, and CLAUDE.md rule 9 obliges every later task to append its systems, requirements and intents with honest provenance and run `npm run validate:data`.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T15:00:00+08:00",
        "sessionRef": "docs/tasks/01-schema-and-seed.md; CLAUDE.md rule 9"
      }
    },
    {
      "id": "req-category-lanes",
      "title": "The diagram reads left-to-right by system role",
      "kind": "functional",
      "status": "implemented",
      "systemIds": ["sys-shared-model", "sys-client-layout", "sys-client-lanes"],
      "evidence": [
        "shared/src/schema/system.ts (category)",
        "client/src/layout/elk.ts (partitionOf)",
        "client/src/model/lanes.test.ts",
        "client/src/layout/elk.test.ts (partition ordering)"
      ],
      "summary": "Every top-level system has a category; ELK lays categories out as columns; a labelled lane is drawn behind each.",
      "detail": "Implemented by task 08-category-lanes. The lane overlay is wired into the canvas as the `overlay` prop of `DiagramCanvas` (task 06).",
      "provenance": {
        "source": "ai-inferred",
        "capturedAt": "2026-08-27T16:00:00+08:00",
        "sessionRef": "agent: category-lanes (task 08)"
      }
    }
  ],
  "intents": [
    {
      "id": "int-intent-first-class",
      "statement": "Intent is a first-class entity, not a field on System.",
      "rationale": "One decision typically explains several systems and edges at once. Modelling it as its own entity with `appliesTo` lets the UI put intent front and center and cross-link it, instead of burying it in a tooltip on one node. Alternatives rejected: A `rationale` string field on each System; Free-text notes attached to edges. Trade-off: More ids to keep consistent by hand; the validator and gaps report exist partly to make that manageable.",
      "status": "active",
      "appliesTo": {
        "systemIds": ["sys-shared-model"],
        "requirementIds": ["req-one-view-understanding"],
        "edgeIds": []
      },
      "summary": "Decisions get their own boxes in the model because one decision usually explains many things.",
      "detail": "The schema has `Intent { statement, rationale, appliesTo: { systemIds, requirementIds, edgeIds } }`. Intent is the only place system/requirement → intent links are stored; `relatedTo` derives the reverse direction.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T15:00:00+08:00",
        "sessionRef": "claude-code session 2026-08-27, planning chat"
      }
    },
    {
      "id": "int-summary-and-detail",
      "statement": "Every entity carries both a one-sentence summary and a deep-dive detail.",
      "rationale": "Different readers need different depth: a newcomer wants plain language, a maintainer wants the full story. Baking both into the schema means the UI can toggle overview/deep dive globally without any entity being left out. Alternatives rejected: A single description field, truncated for overview mode; Generating the summary at render time. Trade-off: Authors must write two texts per entity; the seed is longer to maintain.",
      "status": "active",
      "appliesTo": {
        "systemIds": ["sys-shared-model"],
        "requirementIds": ["req-overview-deep-dive"],
        "edgeIds": []
      },
      "summary": "Simple and deep language are both required fields, not an afterthought.",
      "detail": "`EntityBase` in `shared/src/schema/entity.ts` requires non-empty `summary` and `detail` on System, Requirement and Intent. Edges make both optional to stay lightweight (see int-edges-lightweight).",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T15:00:00+08:00",
        "sessionRef": "claude-code session 2026-08-27, planning chat"
      }
    },
    {
      "id": "int-project-is-own-demo",
      "statement": "The project is its own demo; the seed data is captured from the build process, not mocked.",
      "rationale": "A mocked example codebase would prove nothing about whether the model captures real intent. Recording this repo's own decisions as they are made tests the model for real and gives the demo honest content. Alternatives rejected: A fictional sample project; Analysing a well-known open-source repo. Trade-off: The demo content is small and about a tooling project, which is less flashy than a big product codebase.",
      "status": "active",
      "appliesTo": {
        "systemIds": ["sys-seed-data"],
        "requirementIds": [
          "req-self-contained-demo",
          "req-dogfood-seed",
          "req-written-video-rationale"
        ],
        "edgeIds": ["edge-seed-conforms-shared"]
      },
      "summary": "The demo data is this repo's real history, so the tool is tested on itself.",
      "detail": "CLAUDE.md rule 9 makes appending to `data/project.json` part of every task's definition of done. Provenance distinguishes what the human decided in the planning chat from what an agent inferred while building.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T15:00:00+08:00",
        "sessionRef": "claude-code session 2026-08-27, planning chat"
      }
    },
    {
      "id": "int-react-flow-elk",
      "statement": "Use React Flow (@xyflow/react) with elkjs layout, rather than LikeC4, Cytoscape or Mermaid.",
      "rationale": "Nodes must be rich React components showing requirements and intent as content, so the rendering layer has to be React-native. React Flow gives full control over nodes and edges; elkjs supplies a proper layered layout for nested systems. Alternatives rejected: LikeC4 — too opinionated for an intent-first UI; Cytoscape — canvas-only, no React nodes; Mermaid — static output, no interaction. Trade-off: More layout and interaction code to write ourselves; elkjs adds bundle weight.",
      "status": "active",
      "appliesTo": {
        "systemIds": ["sys-client-app"],
        "requirementIds": ["req-one-view-understanding"],
        "edgeIds": []
      },
      "summary": "A React-native diagram library was chosen so nodes can carry real content, not just labels.",
      "detail": "Both packages are installed in `client/` since the bootstrap commit. The model stores no coordinates; the client computes positions with ELK at render time.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T15:00:00+08:00",
        "sessionRef": "claude-code session 2026-08-27, planning chat"
      }
    },
    {
      "id": "int-isolated-worktrees",
      "statement": "Each parallel agent works in its own git worktree with its own allocated ports.",
      "rationale": "Agents run concurrently and each starts dev servers. Worktrees give file-level isolation and a private branch; the ports allocator prevents `EADDRINUSE` collisions and hardcoded 5173/3001. Alternatives rejected: All agents on one checkout with feature branches; Fixed port offsets per task. Trade-off: A full `npm install` per worktree and some disk use; the human does all merges by hand, fast-forward only.",
      "status": "active",
      "appliesTo": {
        "systemIds": ["sys-agent-workflow", "sys-worktree-scripts", "sys-ports-allocator"],
        "requirementIds": ["req-parallel-agents-safe"],
        "edgeIds": ["edge-dev-runner-calls-ports"]
      },
      "summary": "Isolation by worktree and port so agents never step on each other.",
      "detail": "See `scripts/new-worktree.sh`, `scripts/ports.mjs` and CLAUDE.md rules 1–2. The allocator reads sibling worktrees' `.env.local` files so ports are unique across the whole checkout, not just free on the OS at that instant.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T15:00:00+08:00",
        "sessionRef": "claude-code session 2026-08-27, planning chat"
      }
    },
    {
      "id": "int-trust-provenance-gaps",
      "statement": "Trust is explicit: provenance on every claim, and the tool reports gaps instead of hiding them.",
      "rationale": "A map that mixes verified facts with AI guesses without saying which is which is worse than no map. Showing provenance and an honest 'unexplained' list lets the reader calibrate. Alternatives rejected: Treating all generated content as equally trustworthy; Omitting entities the model cannot explain so the diagram looks complete. Trade-off: The demo will visibly show gaps and `ai-inferred` badges, which looks less polished but is the point.",
      "status": "active",
      "appliesTo": {
        "systemIds": ["sys-shared-model", "sys-seed-data"],
        "requirementIds": ["req-provenance-on-claims"],
        "edgeIds": []
      },
      "summary": "Every claim says who vouched for it, and the model admits what it cannot explain.",
      "detail": "`ProvenanceSchema` is required on every entity. `computeGaps` lists systems without intent, requirements without a system, edges without intent and intents without a target; `validateProject` returns it alongside the parsed project so the server can serve it.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T15:00:00+08:00",
        "sessionRef": "claude-code session 2026-08-27, planning chat"
      }
    },
    {
      "id": "int-chat-last",
      "statement": "Chat and AI generation features come deliberately last.",
      "rationale": "The core question is whether a human can understand a system from the map, not whether an AI can generate text about it. Building understanding first keeps the prototype focused and keeps the demo self-contained with no API keys. Alternatives rejected: Leading with an LLM chat over the codebase; Generating the architecture model from the repo with an LLM in task 01. Trade-off: The first demo has no generative feature, which some reviewers may expect.",
      "status": "active",
      "appliesTo": {
        "systemIds": [],
        "requirementIds": ["req-self-contained-demo"],
        "edgeIds": []
      },
      "summary": "Understanding before generation: no chat until the map works.",
      "detail": "Task 01 explicitly forbade LLM calls and repo analysis; the seed is hand-written. No system exists for chat yet, which is why this intent applies to no system.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T15:00:00+08:00",
        "sessionRef": "claude-code session 2026-08-27, planning chat"
      }
    },
    {
      "id": "int-backend-first",
      "statement": "Build the model and backend first; design the UI look-and-feel separately with Claude Design.",
      "rationale": "The data model is the contract everything depends on and is where the product thesis lives. Locking it first lets server and client tasks run in parallel, while visual design happens in a dedicated tool rather than by iterating CSS in code. Alternatives rejected: UI-first prototyping with hardcoded data; Designing look-and-feel inline in React components. Trade-off: The client is a bare hello page for longer than a UI-first approach would leave it.",
      "status": "active",
      "appliesTo": {
        "systemIds": ["sys-shared-model", "sys-server-api", "sys-client-app"],
        "requirementIds": ["req-single-focused-interaction"],
        "edgeIds": ["edge-client-depends-shared", "edge-server-depends-shared"]
      },
      "summary": "Model, then server, then UI; visual design is a separate track.",
      "detail": "Task order: 01 schema and seed (this), then the server serving the project, then the diagram. Both server and client already depend on `@app/shared` for `HealthStatus`, and will for the whole model.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T15:00:00+08:00",
        "sessionRef": "claude-code session 2026-08-27, planning chat"
      }
    },
    {
      "id": "int-zod-single-definition",
      "statement": "Zod defines the schema once; TypeScript types are inferred from it.",
      "rationale": "Runtime validation at the API boundary and compile-time types must never drift. `z.infer` gives both from one definition. It is the one runtime dependency `shared/` is allowed. Alternatives rejected: Hand-written interfaces plus a separate JSON Schema; TypeScript types only, trusting the JSON. Trade-off: One dependency in the otherwise dependency-free contract package.",
      "status": "active",
      "appliesTo": {
        "systemIds": ["sys-shared-model"],
        "requirementIds": [],
        "edgeIds": []
      },
      "summary": "One schema definition serves both validation and types.",
      "detail": "See `shared/src/schema/`. `shared/package.json` depends on `zod@^3`; nothing else.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T15:00:00+08:00",
        "sessionRef": "docs/tasks/01-schema-and-seed.md (Context: 'Zod for schemas')"
      }
    },
    {
      "id": "int-edges-lightweight",
      "statement": "Edges have optional summary/detail and no provenance of their own; their explanation lives in the Intent they point to.",
      "rationale": "An edge is a fact about two systems that already carry provenance. Requiring two paragraphs and a provenance block per arrow would make the seed tedious and push authors to omit edges. Keeping edges thin and hanging the 'why' on `intentId` keeps intent as the single place decisions are explained. Alternatives rejected: Edges as full entities with required summary, detail and provenance; Edges with no explanatory fields at all. Trade-off: An edge without an `intentId` is unexplained, which the gaps report surfaces as `edgesWithoutIntent`.",
      "status": "active",
      "appliesTo": {
        "systemIds": ["sys-shared-model"],
        "requirementIds": [],
        "edgeIds": []
      },
      "summary": "Arrows stay thin; the reason for an arrow is its intent.",
      "detail": "`EdgeSchema` in `shared/src/schema/edge.ts`: `id, from, to, kind, label?, intentId?, summary?, detail?`. The brief left this to the implementing agent, so it is marked as AI-inferred.",
      "provenance": {
        "source": "ai-inferred",
        "capturedAt": "2026-08-27T15:00:00+08:00",
        "sessionRef": "task 01-schema-and-seed: agent decision, brief said 'your call'"
      }
    },
    {
      "id": "int-tests-pin-intent",
      "statement": "Every module ships with a small test that pins its intent, not its implementation.",
      "rationale": "With several agents editing in parallel, the danger is a silent behaviour change in something another task relies on. A test stating what the module must do fails loudly at `finish-worktree.sh` time. Coverage is not the goal. Alternatives rejected: Coverage thresholds; No tests during the prototype phase. Trade-off: Some duplication between tests and briefs; tests stay deliberately shallow.",
      "status": "active",
      "appliesTo": {
        "systemIds": ["sys-check-suite"],
        "requirementIds": ["req-parallel-agents-safe"],
        "edgeIds": []
      },
      "summary": "Tests exist to stop other agents breaking you, not to hit a number.",
      "detail": "CLAUDE.md rule 6 states this. Marked AI-inferred because the rule was found in the repo, not confirmed in the planning conversation enumerated for this task.",
      "provenance": {
        "source": "ai-inferred",
        "capturedAt": "2026-08-27T15:00:00+08:00",
        "sessionRef": "CLAUDE.md rule 6; task 01-schema-and-seed"
      }
    },
    {
      "id": "int-minimal-phase1-schema",
      "statement": "Schema kept minimal for phase 1; links stored once and derived; everything cut is re-addable as optional fields.",
      "rationale": "With several agents appending to the seed in parallel, bidirectional links (System.intentIds mirroring Intent.appliesTo, Requirement.intentIds mirroring the same) drift: one side gets updated and the other does not, and the validator cannot tell which side is right. Storing each link once — Requirement.systemIds, Intent.appliesTo, Edge endpoints/intentId, System.parentId — and deriving the reverse in relatedTo removes that failure mode. Intent.alternativesRejected and tradeoffs were folded into rationale prose for now. Every removed field can return later as an optional field or enum value without breaking existing data; the backlog in docs/TASKS.md lists them.",
      "status": "active",
      "appliesTo": {
        "systemIds": ["sys-shared-model", "sys-seed-data"],
        "requirementIds": ["req-parallel-agents-safe"],
        "edgeIds": []
      },
      "summary": "Store each link once and derive the other direction, so parallel agents cannot leave the two sides disagreeing.",
      "detail": "Removed in this revision: System.requirementIds, System.intentIds, Requirement.intentIds, Intent.alternativesRejected, Intent.tradeoffs. Added: Intent.status ('active' | 'superseded') + supersededBy?, validated to agree. Kept: the common base (id, summary, detail, provenance), Edge shape, Project shape. `relatedTo` derives system → requirements/intents and requirement → intents; `computeGaps` treats a system as explained when any intent's appliesTo names it.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T16:00:00+08:00",
        "sessionRef": "claude-code session 2026-08-27, planning chat"
      }
    },
    {
      "id": "int-server-thin-validate-once",
      "statement": "The server validates the project once at boot and stays thin; a broken seed is surfaced, not hidden.",
      "rationale": "The server's whole job is load, validate, serve. Validating once and caching keeps the one project route a pure read of the cached model, and keeps analysis or AI out of the server so it can be added as a separate task later. If the seed fails validation the server must still boot: `/api/project` returns 500 with the readable path+message errors and `/api/health` reports `loaded: false`, so an author sees exactly what is wrong instead of a crash loop or a silently empty diagram. Alternatives rejected: crashing at startup on an invalid file; validating on every request; serving whatever parses even if references dangle. Trade-off: editing the seed needs `POST /api/project/reload` (dev only) or a restart.",
      "status": "active",
      "appliesTo": {
        "systemIds": ["sys-server-api"],
        "requirementIds": ["req-self-contained-demo", "req-provenance-on-claims"],
        "edgeIds": ["edge-server-depends-shared", "edge-server-reads-seed"]
      },
      "summary": "Load once, validate once, serve; if the data is broken, say so loudly.",
      "detail": "`server/src/stores/projectStore.ts` holds the `ValidateResult` for the process; routes read it through `store.current()`. `projectStore.test.ts` pins that an invalid fixture still boots the app with a 500 on `/api/project`.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T15:10:00+08:00",
        "sessionRef": "docs/tasks/02-api.md (Scope: projectStore; Goal)"
      }
    },
    {
      "id": "int-selection-client-side",
      "statement": "The selection closure is computed client-side from the shared model, not via the API.",
      "rationale": "Highlighting must be instant and there should be one source of truth: `relatedTo` in `@app/shared` runs in the browser over the already-loaded project. The per-entity API routes were removed (see int-api-single-payload); the UI only ever had the whole model.",
      "status": "active",
      "appliesTo": {
        "systemIds": ["sys-client-state", "sys-shared-model"],
        "requirementIds": ["req-single-focused-interaction"],
        "edgeIds": ["edge-client-depends-shared"]
      },
      "summary": "Selection highlight is computed in the browser with relatedTo.",
      "detail": "Client state holds only the selected id; everything derived (related ids, highlighted/dimmed) is a pure function of project + id so it is trivially testable and never stale.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T15:00:00Z",
        "sessionRef": "task 03-client-foundation brief"
      }
    },
    {
      "id": "int-layout-pure-pipeline",
      "statement": "Layout is a pure pipeline: model → flow elements → ELK positions.",
      "rationale": "The design pass restyles nodes without touching logic: `toFlowElements` and `layoutWithElk` take model data and return plain data, so custom node components and tokens can be swapped in on top. React Flow 12 needs parents before children and parent-relative child positions; the pipeline guarantees both.",
      "status": "active",
      "appliesTo": {
        "systemIds": ["sys-client-layout", "sys-client-app"],
        "requirementIds": ["req-one-view-understanding"],
        "edgeIds": []
      },
      "summary": "Model to positioned nodes is pure data transformation.",
      "detail": "No component computes layout; `useLayout` is the only effectful wrapper and merely resolves the async ELK call and reports status.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T15:00:00Z",
        "sessionRef": "task 03-client-foundation brief"
      }
    },
    {
      "id": "int-api-single-payload",
      "statement": "The API is one payload: the whole model in one call; the client derives selection, highlighting and gaps locally",
      "rationale": "The model is small and the product's point is seeing it all at once, so there is nothing to gain from paging or per-entity fetches. Holding one immutable object on the client means one source of truth: selection closures (`relatedTo`) and gaps (`computeGaps`) are derived from it with the same `@app/shared` functions the validator uses, so the UI can never disagree with the data. The API response is byte-identical in shape to `data/project.json`, which is a trust property: what you see in the diagram is what is in the file. The original task 02 added `GET /api/project/related/:id`, `/entities/:id` and `/gaps`; human review found they duplicated client logic for a hypothetical curl user, and they were removed.",
      "status": "active",
      "appliesTo": {
        "systemIds": ["sys-server-api", "sys-client-app"],
        "requirementIds": [],
        "edgeIds": ["edge-client-calls-server"]
      },
      "summary": "One GET, the whole model; everything else is computed on the client.",
      "detail": "Remaining routes: `GET /api/project`, `GET /api/health`, dev-only `POST /api/project/reload`. Removed in 02b-api-trim: `services/entities.ts`, `lib/slug.ts` and the three per-entity routes.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T18:00:00+08:00",
        "sessionRef": "claude-code session 2026-08-27, planning chat (API review)"
      }
    },
    {
      "id": "int-design-tokens-single-source",
      "statement": "All visual values come from the Organic token sheet designed in Claude Design; components never hard-code colors or fonts, so restyling never touches logic",
      "rationale": "The look was designed once, by the human, in Claude Design (\"Project Atlas\"). Porting its `:root` block verbatim into `client/src/styles/tokens.css` and deriving every app-specific color (system kinds, requirement status, provenance, canvas) from the existing ramps means a restyle is an edit to one file. Components and features reference `var(--…)` or base classes only; `grep` for hex literals in `client/src` must stay empty.",
      "status": "active",
      "appliesTo": {
        "systemIds": ["sys-client-styles"],
        "requirementIds": [],
        "edgeIds": []
      },
      "summary": "One token sheet is the visual source of truth; no hex, font or radius in component code.",
      "detail": "Semantic tokens: `--kind-ui/service/module/store/external/workflow`, `--status-implemented/partial/planned`, `--prov-human/ai`, `--dim-opacity`, `--canvas-bg/dot/dot-size`. Dark theme tokens are backlog.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T20:00:00+08:00",
        "sessionRef": "Claude Design project 'Project Atlas' - planning chat 2026-08-27"
      }
    },
    {
      "id": "int-category-is-classification",
      "statement": "Category is a lane (classification), separate from parentId (containment); lanes give the diagram a left-to-right reading order",
      "rationale": "Two different questions get two different fields. `parentId` says what a system is inside of; `category` says what role it plays (Model, Server, Client, Workflow) and therefore which column it belongs in. Folding category into the tree (e.g. a synthetic parent per category) was rejected because it would force every top-level system into an artificial box and break real containment. Only top-level systems carry a category; nested systems inherit their ancestor's, so they always land inside their parent's lane. ELK's `partitioning` option turns each category into a vertical column ordered by first appearance in `project.systems`, which makes the reading order a property of the data, not of the layout engine.",
      "status": "active",
      "appliesTo": {
        "systemIds": ["sys-shared-model", "sys-client-layout", "sys-client-lanes"],
        "requirementIds": ["req-category-lanes"],
        "edgeIds": []
      },
      "summary": "`category` = which lane; `parentId` = which box. Lanes are ELK partitions in data order.",
      "detail": "Schema: `System.category?: string` (additive). Layout: `elk.partitioning.activate` on the root, `elk.partitioning.partition: <lane index>` on each top-level node, direction RIGHT. Rendering: `LaneLayer` in `client/src/features/diagram/components/` (moved there by task 06 so features never import each other). Out of scope: lane collapsing, filtering, per-lane colours, editing categories.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T16:00:00+08:00",
        "sessionRef": "planning chat 2026-08-27, design review"
      }
    },
    {
      "id": "int-panel-always-visible",
      "summary": "The right panel never goes empty: project overview by default, entity detail on selection.",
      "detail": "Overrides the Claude Design mock, which hid the panel until a click. `SidePanel` takes both views as slots and switches on `selectedId`, so later panel tasks fill content without touching the frame.",
      "statement": "The right panel always shows something: the whole project when nothing is selected, the entity when something is",
      "rationale": "Newcomers need the overview before they click; no empty states. The design hid the panel until a selection was made — the human overrode that so the screen always answers 'what is this project?' or 'what is this thing?'.",
      "status": "active",
      "appliesTo": {
        "systemIds": ["sys-client-shell"],
        "requirementIds": [],
        "edgeIds": []
      },
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T16:00:00+08:00",
        "sessionRef": "planning chat 2026-08-27, design review"
      }
    },
    {
      "id": "int-global-view-mode",
      "summary": "One Overview / Deep dive switch in the header drives every card and panel.",
      "detail": "Overrides the per-card button in the design mock. The `.seg` control in `Header` binds to `useViewMode()`, which persists the choice in localStorage so a reload keeps the reader at the same altitude.",
      "statement": "Overview vs deep dive is one global switch, not per card",
      "rationale": "summary/detail is baked into the schema for every entity; one switch keeps the whole screen at one altitude. The design had a per-item button; the human moved it to the header, bound to `useViewMode()`, which persists across reloads.",
      "status": "active",
      "appliesTo": {
        "systemIds": ["sys-client-shell", "sys-client-state"],
        "requirementIds": [],
        "edgeIds": []
      },
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T16:00:00+08:00",
        "sessionRef": "planning chat 2026-08-27, design review"
      }
    },
    {
      "id": "int-panel-answers-three-questions",
      "statement": "Selecting a system answers, in one panel: what it is, what it must do (requirements + evidence), and why it is built this way (intents + rationale)",
      "rationale": "The product thesis is that intent and requirements are content, not tooltips. One panel with a fixed order — what, must-do, why — means a newcomer reads a system the same way every time, and the evidence and rationale sit right next to the claim they support instead of behind a click. Requirements and intents re-center the same panel so the reading flow never changes shape.",
      "status": "active",
      "appliesTo": {
        "systemIds": ["sys-client-panel"],
        "requirementIds": ["req-single-focused-interaction", "req-overview-deep-dive"],
        "edgeIds": []
      },
      "summary": "One panel, three questions, always in the same order: what, what it must do, why.",
      "detail": "Headings are fixed per entity type: a system shows \"Requirements\" and \"Why it is built this way\"; an intent shows \"Requirements it serves\"; a requirement shows \"Intents\". The global overview / deep-dive switch decides whether cards show summary or detail and rationale; there is no per-item deep-dive button.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T16:00:00+08:00",
        "sessionRef": "planning chat 2026-08-27, design review"
      }
    },
    {
      "id": "int-node-is-a-card",
      "statement": "A system on the canvas is a rich card (summary, counts of requirements and intents, provenance), not a labeled box",
      "rationale": "The product thesis is that intent and requirements are front-and-center content, not tooltips. If a node is only a name, the reader has to click every box to learn anything; a card that already shows what the system does, how much is asked of it (N req), how many decisions shaped it (N why) and whether a human vouched for it lets the whole diagram be read at a glance and makes the click a deepening rather than a discovery. The cost is a larger node and a fixed-size layout placeholder, accepted for a small model.",
      "status": "active",
      "appliesTo": {
        "systemIds": ["sys-client-diagram"],
        "requirementIds": ["req-single-focused-interaction"],
        "edgeIds": []
      },
      "summary": "Every system card carries its summary, its counts and its provenance so the diagram reads without clicking.",
      "detail": "Card spec from the Claude Design 'Project Atlas' work: 210px wide, kind dot + Caprasimo name, summary (overview) or detail (deep dive), tag row `kind · N req · N why · Verified/AI`. Selection states: ring + accent-200 fill when selected, accent-100 fill when related, `--dim-opacity` otherwise. Parent systems are a lighter frame with a legend label rather than a card.",
      "provenance": {
        "source": "human-verified",
        "capturedAt": "2026-08-27T16:00:00+08:00",
        "sessionRef": "Claude Design 'Project Atlas' + planning chat 2026-08-27"
      }
    }
  ],
  "edges": [
    {
      "id": "edge-client-calls-server",
      "from": "sys-client-app",
      "to": "sys-server-api",
      "kind": "calls",
      "label": "GET /api/project",
      "summary": "The client fetches `/api/project` once at startup and parses it with `validateProject`; Vite proxies `/api` to the Fastify port.",
      "intentId": "int-api-single-payload"
    },
    {
      "id": "edge-client-depends-shared",
      "from": "sys-client-app",
      "to": "sys-shared-model",
      "kind": "depends",
      "intentId": "int-backend-first",
      "summary": "The client imports types (today `HealthStatus`) from `@app/shared` via a Vite alias to `shared/src`."
    },
    {
      "id": "edge-server-depends-shared",
      "from": "sys-server-api",
      "to": "sys-shared-model",
      "kind": "depends",
      "intentId": "int-backend-first",
      "summary": "The server validates the seed with `validateProject` and types its responses with `@app/shared`."
    },
    {
      "id": "edge-seed-conforms-shared",
      "from": "sys-seed-data",
      "to": "sys-shared-model",
      "kind": "depends",
      "intentId": "int-project-is-own-demo",
      "label": "validated by",
      "summary": "`npm run validate:data` runs `validateProject` over the seed; it must stay green."
    },
    {
      "id": "edge-dev-runner-calls-ports",
      "from": "sys-dev-runner",
      "to": "sys-ports-allocator",
      "kind": "calls",
      "intentId": "int-isolated-worktrees",
      "summary": "`dev.mjs` runs `ports.mjs` before starting servers so ports are always allocated."
    },
    {
      "id": "edge-server-reads-seed",
      "from": "sys-server-api",
      "to": "sys-seed-data",
      "kind": "reads",
      "label": "PROJECT_FILE",
      "intentId": "int-server-thin-validate-once",
      "summary": "The server reads `data/project.json` at boot (path overridable with `PROJECT_FILE`) and on `POST /api/project/reload` in dev."
    }
  ]
}
